export const TODO_WRITE_DESCRIPTION =
  'Track structured progress on multi-step or multi-item tasks. Pass the COMPLETE desired todo list each call; previous state is replaced. Each item: { id, content, status: "pending" | "in_progress" | "completed" }. Max one item in_progress at a time. Use this when the task has 3+ steps so you can see what is left without scanning prior turns.';
