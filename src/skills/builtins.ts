import type { Skill } from './types';

// Bundled skills registry reserved for future use (doc §11 out of scope for now).
export const BUILTIN_SKILLS: readonly Skill[] = [];
export const BUILTIN_NAMES: ReadonlySet<string> = new Set();
