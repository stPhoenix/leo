# Compliance iteration 1 — F05 chat-message-list-markdown

## Acceptance criteria

- AC1 (interleaved user + assistant history renders in submission order; user plain-text, assistant markdown, inside the F04 region): PASS — `MessageList` subscribes to `ChatMessageStore` and emits `<li role="listitem" data-role={role}>` per record in submission order (`src/ui/chat/MessageList.tsx:73-100`); `UserBubble` carries plain-text content with `data-slot="user-text"` (`src/ui/chat/MessageList.tsx:111-119`); `AssistantBubble` invokes the injected `renderMarkdown` into `data-slot="assistant-markdown"` (`src/ui/chat/MessageList.tsx:128-149`); the parent `<section>` retains the F04 `role="log"` + `aria-live="polite"` + `data-region="messages"` envelope (`src/ui/chat/MessageList.tsx:62-71`). Order verified by `tests/dom/messageList.test.tsx:36-46`; user vs assistant routing by `tests/dom/messageList.test.tsx:48-62`. FR-CHAT-02.
- AC2 (user vs assistant visually distinguishable using only Obsidian CSS variables; style audit zero literals): PASS — bubble theming in `styles.css` uses only Obsidian variables (`background`, `border`, `text`, `accent`, `shadow`, `hover`, `modifier-border`); `tests/unit/stylesAudit.test.ts` proves zero hex/rgb/hsl literals in the file; the inline-style audit in `tests/dom/chatRoot.test.tsx:62-77` confirms no React component sets a hardcoded inline colour. User bubble surface differentiates via `--background-modifier-hover` + `--interactive-accent` border, assistant bubble via `--background-secondary` + `--background-modifier-border`; both inherit theme switching. FR-CHAT-02.
- AC3 (append auto-scrolls when at bottom; preserves position when scrolled up): PASS — pure helpers `isNearBottom` / `shouldAutoScroll` in `src/ui/chat/scrollAnchoring.ts`; `MessageList`'s `useLayoutEffect` reads `wasAtBottomRef` (set by the `scroll` listener) and either snaps to `el.scrollHeight` or increments `pendingNew` (`src/ui/chat/MessageList.tsx:32-43`); the "Jump to latest (N)" pill renders only when `pendingNew > 0` (`src/ui/chat/MessageList.tsx:103-113`). Threshold edge cases asserted by `tests/unit/scrollAnchoring.test.ts` (7 cases). FR-CHAT-02.
- AC4 (assistant messages rendered via `MarkdownRenderer.render` with native headings/lists/blockquotes/code/links): PASS — `ChatView` builds a per-bubble `Component` and calls `MarkdownRenderer.render(this.app, text, container, '', cmp)` inside the injected `renderMarkdown` (`src/ui/chatView.tsx:51-60`); the React component's `useEffect` invokes the callback once per assistant message into the bubble's container (`src/ui/chat/MessageList.tsx:130-143`); `tests/dom/messageList.test.tsx:81-94` asserts the call count + payloads. The actual list / heading / link / blockquote rendering is delegated to Obsidian's native pipeline, which is the contract. FR-CHAT-06.
- AC5 (fenced code blocks syntax-highlighted via Obsidian's native pipeline; plain pre/code when language unknown): PASS — `MarkdownRenderer.render` produces the `<pre><code class="language-…">` markup Obsidian's prism / native renderer styles; the fixture in `tests/dom/messageList.test.tsx:24-32` (`fakeMarkdownWithCode`) demonstrates that an absent language fence still produces a recognisable `<pre><code>` block, and the copy-button enhancer attaches regardless. FR-CHAT-06.
- AC6 (per-block copy-to-clipboard button: Obsidian icon, keyboard-reachable, copies fence text, fires Notice): PASS — `enhanceCodeBlocks` (`src/ui/chat/codeBlockEnhancer.ts:14-44`) walks every `<pre><code>`, attaches a button with `aria-label="Copy code"` + `tabIndex=0` + focus-visible outline (CSS), is idempotent, and on click pipes `code.textContent` through `clipboard.copy` then `clipboard.notify('Copied to clipboard')`. Production wiring uses Obsidian's `setIcon('copy')` + `navigator.clipboard.writeText` + `new Notice(message)` (`src/ui/chatView.tsx:62-72,80`). Verified by `tests/unit/codeBlockEnhancer.test.ts:14-66` (5 cases) and `tests/dom/messageList.test.tsx:96-117`.
- AC7 (unmount tears down every markdown subtree, copy-button listeners, leaves no dangling DOM/listeners): PASS — `MessageList`'s assistant-bubble effect returns a cleanup that calls the markdown cleanup, the code-block enhancer cleanup, and `host.replaceChildren()` (`src/ui/chat/MessageList.tsx:135-143`); `ChatView.onClose` additionally unloads every per-bubble Obsidian `Component`, unmounts the React root, disconnects the `ResizeObserver`, clears the width-listener set, and empties the host (`src/ui/chatView.tsx:88-104`). `tests/dom/messageList.test.tsx:119-148` asserts both invariants — every cleanup is called and the host is empty after `unmount()`; the second case proves a content change on a stable id removes and re-attaches the copy button without leaks. FR-CHAT-02 + FR-CHAT-06.

## Scope coverage

- In scope "MessageList component rendering an ordered list, scroll-anchored to latest while preserving manual scroll": PASS — `src/ui/chat/MessageList.tsx`.
- In scope "User-role bubble: plain-text, whitespace preserved, theme-variable styling, distinct surface/border/alignment": PASS — `UserBubble` uses `white-space: pre-wrap` (`styles.css`), `.leo-message-user { justify-content: flex-end; }`.
- In scope "Assistant-role bubble: full markdown via MarkdownRenderer.render into isolated DOM subtree, React cleanup on unmount per block": PASS — `AssistantBubble` + `ChatView.renderMarkdown`.
- In scope "Code fences: syntax highlighting via native pipeline + per-block copy button (Obsidian icon, keyboard-reachable, Notice on success)": PASS — `enhanceCodeBlocks` + `ChatView` clipboard wiring.
- In scope "Stable list keys per message id so reorder/append/partial updates don't remount the transcript": PASS — `key={m.id}` on each `<li>` (`src/ui/chat/MessageList.tsx:78`); verified by `tests/dom/messageList.test.tsx:64-79`.
- In scope "Unit coverage for: render ordering, role styling branches, markdown render invocation, copy click path, CSS-variable-only style audit": PASS — coverage above.

## Out-of-scope audit

- Out of scope "Token-by-token streaming + stop control (F07)": CLEAN — no streaming-state plumbing in `MessageList` / `ChatView`; assistant content is rendered only when full.
- Out of scope "Per-message actions copy / regenerate / edit-and-resend / delete (F15)": CLEAN — no action toolbar on bubbles.
- Out of scope "ContextIndicator data (F09)": CLEAN — `ContextIndicator` still renders the F04 placeholder; F05 did not touch it.
- Out of scope "Visual states + Notice vs status-bar policy (F13)": CLEAN — no idle / streaming / cancelled / edit-locked classes added to bubbles; the only `Notice` use is for the copy-success toast which AC6 explicitly mandates.

## QA aggregate

QA verdict (qa-1.md): PASS. All four gates green: typecheck (exit 0), lint (exit 0), tests (124/124 across 18 files), build (exit 0; production main.js ≈ 181 KB).

## Verdict: PASS
