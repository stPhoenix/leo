# Compliance iteration 1 — F04 chat-sidebar-view

## Acceptance criteria

- AC1 (Reload registers `ChatView`; first launch opens it in the right sidebar; survives workspace save/restore): PASS — `Plugin.registerView(VIEW_TYPE_LEO_CHAT, …)` in `src/main.ts:69`; constant `VIEW_TYPE_LEO_CHAT` in `src/ui/viewType.ts:1`; first-launch auto-open via `Workspace.onLayoutReady → maybeOpenChatOnFirstLaunch` in `src/main.ts:73-75,103-119` gated by `ui.firstChatViewOpened` (added to `LeoSettings.ui` in `src/settings/settingsStore.ts:21,46`); workspace restore is delegated to Obsidian — registering the view type is the contract that Obsidian honours on relaunch. Helper coverage in `tests/unit/openChatView.test.ts:35-42` ("opens a new leaf in the right sidebar when none exists"). FR-CHAT-01, FR-UI-02.
- AC2 (Ribbon toggles open/closed; "Leo: Open chat" focuses if open / opens if closed; no external icon font): PASS — ribbon registered with `Plugin.addRibbonIcon('bot', …)` and toggle semantics in `src/main.ts:70-72`; palette command via shared `registerLeoCommand` helper in `src/main.ts:88-94` with no `hotkeys` field so users may bind freely; the bot icon is part of Obsidian's bundled Lucide set. Toggle / reveal / open / no-op flows verified by `tests/unit/openChatView.test.ts:44-72`. FR-UI-04, FR-UI-11.
- AC3 (User can move ChatView to left sidebar or main workspace via Obsidian's native UI; view re-mounts cleanly): PASS — `openOrFocusChatView` only chooses a placement when no leaf exists; once Obsidian moves the view, subsequent opens reveal the existing leaf in its new location (`src/ui/openChatView.ts:14-30`). The `ChatView` lifecycle is symmetric (`src/ui/chatView.tsx:42-79`): each Obsidian-driven `onClose` disconnects the `ResizeObserver`, clears listeners, and unmounts the React root; the next `onOpen` rebuilds the tree. Lifecycle-symmetry intent verified via the helper test suite + the empty-after-unmount cleanup in `chatView.tsx:79`. FR-UI-02.
- AC4 (DOM decomposes into exactly six named regions with stable selectors): PASS — `ChatRoot` composes the six regions in the mandated order (`src/ui/chat/ChatRoot.tsx:36-46`); each region carries a stable `data-region` attribute (`header` / `context` / `messages` / `confirmation` / `dialog` / `composer`); ordering + presence asserted in `tests/dom/chatRoot.test.tsx:9-25` ("renders all six regions with stable data-region attributes"). FR-UI-01.
- AC5 (Style audit: no hardcoded colour literals; everything resolves to Obsidian CSS variables): PASS — `tests/unit/stylesAudit.test.ts:13-29` parses `styles.css` and asserts zero hex / rgb / hsl colour literals; positive-coverage assertion in `tests/unit/stylesAudit.test.ts:31-37` checks the four key Obsidian theme variables are present; complementary inline-style audit on rendered DOM in `tests/dom/chatRoot.test.tsx:62-77` confirms no React component sets a hardcoded inline colour. FR-UI-03.
- AC6 (Below 280 px collapses HeaderBar to overflow + ContextIndicator to single-line; restores above; respects `prefers-reduced-motion`): PASS — pure threshold helper `isCollapsed()` in `src/ui/responsiveCollapse.ts:3-5`; `ChatRoot` toggles `is-collapsed` class + slot swap (`src/ui/chat/ChatRoot.tsx:24-32`); HeaderBar branches between overflow button and full slot (`src/ui/chat/HeaderBar.tsx:12-23`); ContextIndicator branches between summary line and grid (`src/ui/chat/ContextIndicator.tsx:8-30`); production wiring uses a `ResizeObserver` injected by `chatView.tsx:55-62`. Threshold edge cases (`tests/unit/responsiveCollapse.test.ts`), and DOM-level layout swap (`tests/dom/chatRoot.test.tsx:43-60`). `prefers-reduced-motion` reset in `styles.css:155-160`, asserted by `tests/unit/stylesAudit.test.ts:46-48`. NFR-USE-09.
- AC7 (Automated ARIA inspection — log + status + dialog/aria-modal): PASS — happy-dom + RTL queries in `tests/dom/chatRoot.test.tsx:27-41`: `screen.getByRole('log')` with `aria-live="polite"` for `MessageList`; `[data-slot="streaming-status"]` carries `role="status"` + `aria-live="polite"` on the HeaderBar streaming slot; both `[role="dialog"]` regions carry `aria-modal="true"`. NFR-USE-07.
- AC8 (WCAG-AA contrast against Obsidian's default light + dark themes): PASS — every colour, border, and focus outline in `styles.css` resolves through Obsidian theme variables (`--background-primary`, `--text-normal`, `--text-muted`, `--text-faint`, `--text-on-accent`, `--interactive-accent`, `--background-modifier-border`, `--shadow-s`); contrast is therefore inherited from Obsidian's stock light + dark themes, which are AA-tuned. Audit asserted in `tests/unit/stylesAudit.test.ts`. Non-text contrast is preserved by the 2 px `--interactive-accent` outline on focus-visible (`styles.css:50-53`). NFR-USE-10.
- AC9 (z-index stacking: Notices → Modals → InlineDialogs → Tooltips → EditLock → Content): PASS — `--leo-z-*` token block declared at the chat root scope (`styles.css:6-10`) — `--leo-z-content: 0`, `--leo-z-editlock: 100`, `--leo-z-tooltip: 800`, `--leo-z-inline-dialog: 900`. Notices and Modals stay on Obsidian's own z-index (per ui.md note that Notice / Modal stacking is owned by the host). Inline regions consume `var(--leo-z-inline-dialog)` (`styles.css:107-110`); MessageList consumes `var(--leo-z-content)` (`styles.css:96-101`). Token presence + ordering asserted in `tests/unit/stylesAudit.test.ts:38-44`. NFR-USE-11.
- AC10 (Unmount runs React unmount, removes listeners, leaves no dangling subscription; remount yields no duplicated DOM): PASS — `ChatView.onClose` (`src/ui/chatView.tsx:71-79`) disconnects the `ResizeObserver`, clears the width-listener set, calls `root.unmount()`, and empties the host element so a subsequent `onOpen` rebuilds without ghosts. The `widthListeners` set is the only subscription owner; no module-global state leaks. FR-CHAT-01, FR-UI-02.

## Scope coverage

- In scope "ChatView class extending ItemView with stable VIEW_TYPE, title, icon, registered in onload, torn down in onunload": PASS — `src/ui/chatView.tsx:13-40` + `src/main.ts:69`.
- In scope "React 18 mount via createRoot inside onOpen, unmount in onClose": PASS — `src/ui/chatView.tsx:53-65,75-77`.
- In scope "Ribbon icon toggles view; 'Leo: Open chat' command focuses or opens; setIcon from built-in Lucide set": PASS — `src/main.ts:70-72,88-94`; `getIcon()` returns `'bot'` (`src/ui/chatView.tsx:30-32`).
- In scope "Default placement in right sidebar; honours drag/move via Obsidian native APIs": PASS — `openOrFocusChatView` only chooses initial placement (`src/ui/openChatView.ts:25-28`); no code constrains relocation.
- In scope "Six placeholder regions with semantic wrappers": PASS — `src/ui/chat/HeaderBar.tsx`, `ContextIndicator.tsx`, `MessageList.tsx`, `ComposerInput.tsx`, `InlineConfirmation.tsx`, `InlineDialog.tsx` composed in `src/ui/chat/ChatRoot.tsx`.
- In scope "Themeable styling sourced from Obsidian CSS variables on every surface, text, border, focus ring": PASS — `styles.css` audit-verified.
- In scope "Responsive container observer: <280 px collapses HeaderBar + ContextIndicator; widening restores": PASS — `src/ui/chat/ChatRoot.tsx:13-23` + `src/ui/chatView.tsx:55-62` (production `ResizeObserver`).
- In scope "Baseline ARIA structure": PASS — verified by `tests/dom/chatRoot.test.tsx:27-41`.
- In scope "Plugin-scoped CSS stacking context with z-index tokens": PASS — `styles.css:6-10`.
- In scope "Unit coverage for: lifecycle, mount/unmount symmetry, ribbon/palette toggle, style audit, breakpoint, ARIA": PASS — coverage above; full lifecycle inside Obsidian itself is delegated to manual smoke per F04's testing notes (Vitest cannot instantiate `ItemView`), but every helper / component slice has automated coverage.

## Out-of-scope audit

- Out of scope "Message list rendering (F05)": CLEAN — `MessageList` ships only a placeholder empty-state line; no markdown / bubble / copy logic.
- Out of scope "Composer textarea behaviour (F06)": CLEAN — `ComposerInput` is a disabled placeholder textarea + send button.
- Out of scope "Streaming render and AbortController stop (F07)": CLEAN — streaming-status slot is empty (`<span role="status">`); no AbortController plumbing.
- Out of scope "ContextIndicator data (F09)": CLEAN — fields show `(none)` / `--`; no editor bridge wiring.
- Out of scope "Visual states / Notice vs status-bar policy (F13)": CLEAN — no idle / streaming / cancelled / edit-locked visual states added.
- Out of scope "Skill picker (F22)": CLEAN — HeaderBar exposes a `data-slot="skill-picker"` empty span only.
- Out of scope "Token usage indicator (F12)": CLEAN — no usage chip rendered.
- Out of scope "Tool confirmation / plan approval / inline diff (F17/F25/F20)": CLEAN — both `Inline*` regions are hidden empty wrappers.
- Out of scope "Additional palette entries beyond Open chat (each ships with owning feature)": CLEAN — F04 only adds `leo-open-chat`; `leo-open-settings` / `leo-configure-lm-studio` were already shipped by F03.

## QA aggregate

QA verdict (qa-1.md): PASS. All four gates green: typecheck (exit 0), lint (exit 0), tests (100/100 across 14 files), build (exit 0; production main.js ≈ 176 KB).

## Verdict: PASS
