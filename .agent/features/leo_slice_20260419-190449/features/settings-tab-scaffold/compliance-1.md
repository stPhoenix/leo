# Compliance iteration 1 — F03 settings-tab-scaffold

## Acceptance criteria

- AC1 (single PluginSettingTab; 7 sections in mandated order; collapsible; collapse state persists across re-opens): PASS — `SettingsTab` extends `PluginSettingTab` (`src/settings/SettingsTab.ts:24`); `display()` iterates `SECTION_ORDER` (`src/settings/settingsStore.ts:159-167`) producing exactly Provider → Indexing → Skills → MCP Servers → Plan/Todos → Appearance → Advanced; per-section header is `role=button` + `aria-expanded` + `aria-controls`, click and Enter/Space both toggle through `SettingsStore.update` writing `ui.expandedSections[id]` (`src/settings/SettingsTab.ts:48-86`); the persisted-shape round-trip is verified by `tests/unit/settingsStore.test.ts:99-117` ("round-trips a save → load cycle") and the unknown-id rejection by `tests/unit/settingsStore.test.ts:71-75` ("preserves only known section ids in expandedSections"). FR-UI-10.
- AC2 (Provider fields editable; persist through saveData; read back after reload): PASS — endpoint/chat/embedding/temperature/max-tokens fields wired to `SettingsStore.update` in `src/settings/SettingsTab.ts:131-189`; the save → load cycle including provider sub-shape is asserted by `tests/unit/settingsStore.test.ts:99-117`; `migrate()` re-applies defaults / clamps on reload (`tests/unit/settingsStore.test.ts:34-58`). FR-PROV-09.
- AC3 (fresh install renders welcome + CTA; wizard probes, lists, picks, saves, clears first-run flag): PASS — `migrate()` infers `firstRunComplete=false` on null/empty plugin data (`src/settings/settingsStore.ts:96-98` + `tests/unit/settingsStore.test.ts:60-65`); `SettingsTab.renderProviderBody` switches to `renderWelcomePanel` when the flag is false (`src/settings/SettingsTab.ts:97-126`); the CTA opens `WizardModal` whose `persist` callback writes the chosen endpoint + models AND flips `firstRunComplete=true` (`src/settings/SettingsTab.ts:128-145`). `wizardMachine` covers every step: endpoint → probing → models / models-empty / probe-failed (`tests/unit/wizardMachine.test.ts:30-79`), models guard, save, persisting → closed and persistError → save (`tests/unit/wizardMachine.test.ts:82-118`). FR-UI-07, NFR-USE-02.
- AC4 (every Leo command palette entry surfaced in Obsidian Hotkeys UI; no hardcoded keys): PASS — `src/settings/commands.ts:8-10` wraps `Plugin.addCommand` via `registerLeoCommand`; the two F03 commands (`Leo: Open settings`, `Leo: Configure LM Studio`) are registered in `src/main.ts:67-77` with no `hotkeys`/`hotkey` field, so Obsidian's native Hotkeys UI exposes them with empty bindings ready for user assignment. The shared helper is the seam every later feature plugs into without duplicating `addCommand` boilerplate. NFR-USE-03.
- AC5 (all user-configurable options reachable from the Leo settings tab; no config-file-only knobs): PASS — endpoint, chat model, embedding model, temperature, max tokens are in the Provider section (`src/settings/SettingsTab.ts:131-189`); `logLevel` is in Advanced (`src/settings/SettingsTab.ts:223-235`); `firstRunComplete` is reachable indirectly via the welcome CTA (when false) and the `Leo: Configure LM Studio` palette command (always). `expandedSections` is implicit UI state. No new `.leo/config.json` fields are introduced — all reads/writes go through `SettingsStore.load/update`. NFR-USE-01.
- AC6 (welcome + wizard is the only first-time setup; subsequent launches skip welcome): PASS — `firstRunComplete` flips to `true` only inside the wizard's `persist` callback (`src/settings/SettingsTab.ts:131-145`); `renderProviderBody` short-circuits to `renderProviderFields` when the flag is true (`src/settings/SettingsTab.ts:97-103`); the `migrate()` rule that an existing legacy install with a stored chat or embedding model is treated as already-complete prevents accidental re-prompting on upgrade (`tests/unit/settingsStore.test.ts:60-65,67-74`). NFR-USE-02 + FR-UI-07.
- AC7 (Skills, MCP Servers, Plan/Todos render as empty collapsible scaffolds with pointers to owning later features): PASS — `SECTION_PLACEHOLDERS` (`src/settings/settingsStore.ts:182-188`) cites F39, F55, and F23–F26 by name; `renderSectionBody` falls through to the placeholder paragraph for any section without a dedicated renderer (`src/settings/SettingsTab.ts:88-95`); the 7-section ordering invariant guarantees later features can mount into existing scaffolds without restructuring. FR-UI-10.

## Scope coverage

- In scope "SettingsTab as a PluginSettingTab rendering seven collapsible sections in the order mandated by FR-UI-10": PASS — `src/settings/SettingsTab.ts` + `SECTION_ORDER` in `src/settings/settingsStore.ts:159-167`.
- In scope "Provider section with live fields bound to plugin data; pickers degrade to free text when unreachable": PASS — `renderProviderFields` + `renderModelPicker` in `src/settings/SettingsTab.ts:131-209`. Free-text fallback flips on `providerManager.connection.isReachable() === false`.
- In scope "First-run detection rendering an empty-state welcome panel with a Configure LM Studio CTA": PASS — `renderProviderBody` + `renderWelcomePanel` in `src/settings/SettingsTab.ts:97-126`; `firstRunComplete` initial inference in `src/settings/settingsStore.ts:91-101`.
- In scope "Setup wizard flow: verify endpoint → probe /v1/models via F02 → pick chat+embedding → save+mark first-run complete; cancellable; persists partial progress": PASS — `wizardMachine` reducer + `WizardModal` + `WizardApp`; persist callback in `SettingsTab.openWizard` flips the flag; cancel from any non-terminal step → `closed` (`tests/unit/wizardMachine.test.ts:121-128`); endpoint edits before cancel persist nothing harmful because the wizard reads the saved value via `initialEndpoint` next time.
- In scope "Hotkey registration for every Leo command surfaced so far through a shared helper": PASS — `registerLeoCommand` in `src/settings/commands.ts`; `main.ts` registers the two F03 commands through it; later features will follow the same call shape.
- In scope "Read of logLevel already written by F01 so the Advanced section can flip it; writes route through saveData": PASS — `src/settings/SettingsTab.ts:223-235` reads the current level, writes the next via `SettingsStore.update` (which calls `saveData`), and re-applies it to the running `Logger` so the change is live without a reload.
- In scope "Plugin data schema (versioned) and a thin settings store layer": PASS — `LeoSettings.schemaVersion: 1` in `src/settings/settingsStore.ts:30-35`; `migrate()` produces v1 from any prior shape; `SettingsStore` is the only writer.

## Out-of-scope audit

- Out of scope "No-index 'Index vault' empty-state CTA + status-bar progress (F30)": CLEAN — the Indexing section renders only its placeholder paragraph; no progress widget added.
- Out of scope "Empty-thread example prompts in chat view (F04)": CLEAN — no chat view introduced.
- Out of scope "Cloud provider adapters, API-key fields, safeStorage wiring (F38)": CLEAN — Provider section only exposes the LM Studio shape; no API-key inputs, no safeStorage import.
- Out of scope "Tool-use OpenAI tools parameter (F16)": CLEAN — no tools UI; provider request body unchanged from F02 (still no `tools` field).
- Out of scope "MCP server CRUD UI inside MCP Servers section (F55)": CLEAN — section body is the F55 placeholder string; no add/edit/delete controls.
- Out of scope "In-plugin skill editor UI (F39)": CLEAN — Skills section body is the F39 placeholder string; no editor mounted.
- Out of scope "Plan/Todos runtime (F23–F26)": CLEAN — Plan/Todos section body is the F23–F26 placeholder string; no plan controls.
- Out of scope "Multi-thread CRUD settings (F37)": CLEAN — no thread-management UI.
- Out of scope "Cost-in-$ display (F38)": CLEAN — Provider section status line shows model count only.

## QA aggregate

QA verdict (qa-1.md): PASS. All four gates green: typecheck (exit 0), lint (exit 0), tests (77/77 across 10 files), build (exit 0; production main.js ≈ 167 KB).

## Verdict: PASS
