# Compliance iteration 1 — F02 provider-lmstudio-core

## Acceptance criteria

- AC1 (POST /v1/chat/completions, SSE → token/usage/done, terminate on signal.aborted): PASS — stream impl `src/providers/lmStudioProvider.ts:43-101`; signal-aborted re-throw `src/providers/lmStudioProvider.ts:90-95`; covered by `tests/integration/lmStudioProvider.test.ts:32-55` ("parses tokens, usage, and [DONE] in order"), `tests/integration/lmStudioProvider.test.ts:57-92` ("forwards model + messages + stream:true"), and `tests/integration/lmStudioProvider.test.ts:94-127` ("terminates cleanly when caller aborts mid-stream"). FR-PROV-01, FR-PROV-03.
- AC2 (GET /v1/models → parsed list): PASS — `src/providers/lmStudioProvider.ts:111-130`; covered by `tests/integration/lmStudioProvider.test.ts:131-148` ("returns parsed model ids from /v1/models") and `tests/integration/lmStudioProvider.test.ts:150-156` ("throws ProviderConnectError on non-2xx"). FR-PROV-02.
- AC3 (FIFO queue serializes; ≤1 in-flight; 120 s per-request timeout): PASS — queue acquire/release `src/providers/providerManager.ts:65-79`, per-attempt timeout AbortController + setTimeout `src/providers/providerManager.ts:88-94`; covered by `tests/integration/providerManager.test.ts:42-72` ("peakActive=1 + enterOrder a,b,c") and `tests/integration/providerManager.test.ts:74-101` ("aborts an attempt when timeoutMs elapses"). FR-PROV-05.
- AC4 (max-3 retry with exponential backoff; 4th persistent failure surfaces Notice + status-bar unreachable): PASS — retry classification `src/providers/providerManager.ts:117-152`, `markUnreachable()` invokes userFacing logger.error `src/providers/providerManager.ts:158-167`, plugin status-bar wiring `src/main.ts:80-87`; covered by `tests/integration/providerManager.test.ts:105-128` ("retries…then succeeds") and `tests/integration/providerManager.test.ts:130-160` ("fourth persistent failure surfaces userFacing error and marks unreachable"). FR-PROV-06.
- AC5 (Provider interface exported; LMStudioProvider implements it without leaking; future adapters register without touching queue/retry): PASS — `Provider` interface `src/providers/types.ts:24-28`; `LMStudioProvider implements Provider` `src/providers/lmStudioProvider.ts:39`; ProviderManager only references `Provider` not `LMStudioProvider` (`src/providers/providerManager.ts:9-21,46-50`) — substituting another `Provider` impl is a constructor-arg swap with zero edits to `attemptWithRetry`/`runStream`/queue. Visible in code; no dedicated test required (design constraint). FR-PROV-07.
- AC6 (EmbeddingClient.embed → POST /v1/embeddings with separate embedding model; chat-model changes do not alter embedding routing): PASS — `src/providers/embeddingClient.ts:84-118`; covered by `tests/integration/embeddingClient.test.ts:13-39` ("POSTs to /v1/embeddings with the embedding model"), `tests/integration/embeddingClient.test.ts:41-60` ("does not share a model with chat — uses its own model fn"), `tests/integration/embeddingClient.test.ts:62-65` ("returns [] for empty input"), and `tests/integration/embeddingClient.test.ts:67-78` ("refuses to call when ConnectionState is unreachable"). FR-PROV-08.
- AC7 (Public unreachable flag disables chat + pauses indexing consumers; subsequent probe success clears state and status-bar): PASS — `ConnectionState` observable `src/providers/connectionState.ts`; chat fast-fail `src/providers/providerManager.ts:67-72`; embedding fast-fail `src/providers/embeddingClient.ts:42-44`; 15 s probe `src/providers/providerManager.ts:169-188`; status-bar binding `src/main.ts:80-87`. Covered by `tests/integration/providerManager.test.ts:163-187` ("fast-fails new streams while unreachable") and `tests/integration/providerManager.test.ts:189-213` ("clears unreachable when the periodic probe succeeds"). NFR-REL-01.
- AC8 (Vitest + msw covers streaming, /v1/models, FIFO, timeout, retry+failure, unreachable entry/exit, embedding routing): PASS — shared msw setup `tests/integration/_mswServer.ts`; all eight required scenarios exercised across `tests/integration/lmStudioProvider.test.ts`, `tests/integration/providerManager.test.ts`, `tests/integration/embeddingClient.test.ts` (12 integration tests, all green in qa-1). NFR-TEST-02.

## Scope coverage

- In scope "Provider TypeScript interface (chat stream, model list, cancellation signal); LMStudioProvider as its first implementation": PASS — `src/providers/types.ts` + `src/providers/lmStudioProvider.ts`.
- In scope "ProviderManager owning the FIFO queue, the 120 s per-request timeout, retry / backoff (max 3 attempts, exponential), and the unreachable state machine that drives a status-bar indicator and Notice": PASS — `src/providers/providerManager.ts` + `src/main.ts:80-87` (status-bar) + userFacing log routing through `Logger.error → Notice + statusEl` from F01.
- In scope "HTTP client wiring (POST /v1/chat/completions stream:true; GET /v1/models; endpoint URL / chat model / embedding model read from plugin data)": PASS — provider HTTP in `src/providers/lmStudioProvider.ts`; settings shape + read in `src/main.ts:11-31,68-88`.
- In scope "Separate EmbeddingClient calling /v1/embeddings so chat and embedding models can be configured independently": PASS — `src/providers/embeddingClient.ts` + `src/main.ts:73-78`.
- In scope "AbortController wiring from caller through fetch so in-flight streams and embedding calls cancel cleanly": PASS — provider passes `signal` straight into `fetch` (`src/providers/lmStudioProvider.ts:55-65,114-117`); ProviderManager links caller signal to per-attempt `AbortController` (`src/providers/providerManager.ts:88-94`); EmbeddingClient links caller signal to per-attempt `AbortController` (`src/providers/embeddingClient.ts:88-118`).
- In scope "Structured log events (provider.request, provider.retry, provider.unreachable, provider.usage) through the F01 Logger": PASS — emitted at `src/providers/providerManager.ts:99` (`provider.request`), `:104` (`provider.usage`), `:142-146` (`provider.retry`), `:160-167` (`provider.unreachable`); EmbeddingClient also drives `provider.retry` / `provider.unreachable` at `src/providers/embeddingClient.ts:58-67,69-74`.
- In scope "Vitest + msw fixture server covering streaming, model listing, queue FIFO order, timeout, retry/backoff, and unreachable-state transition": PASS — shared `tests/integration/_mswServer.ts` plus three integration suites covering all six bullets.

## Out-of-scope audit

- Out of scope "Tool-use via OpenAI tools parameter and tool_calls stream events (F16)": CLEAN — request body in `src/providers/lmStudioProvider.ts:44-52` carries no `tools`/`tool_choice`; `StreamEvent` union in `src/providers/types.ts:18-23` excludes `tool_call`/`tool_confirmation`/`tool_result`.
- Out of scope "Settings-tab UI for endpoint / model / temperature / max-tokens and first-run LM Studio wizard (F03)": CLEAN — no `PluginSettingTab` subclass added; `main.ts` only reads plugin data.
- Out of scope "Cloud providers (OpenAI / Anthropic / Ollama / custom) and safeStorage-backed API keys (F38)": CLEAN — only `LMStudioProvider` implementation; no `safeStorage` import.
- Out of scope "Chat UI streaming render, stop button, 60 fps target (F07)": CLEAN — no UI components added; React not yet introduced.
- Out of scope "Token-usage display and cost-in-$ (F12)": CLEAN — `usage` event is emitted by transport only; no UI for it.
- Out of scope "Agent-level request assembly, truncation, one-in-flight at the agent loop (F10)": CLEAN — no `AgentRunner` / context-assembler. The FIFO here is transport-level inside `ProviderManager`.
- Out of scope "RAG embedding consumer wiring (F29)": CLEAN — `EmbeddingClient` is a transport; no `VaultIndexer` / `VectorStore` wiring.

## QA aggregate

QA verdict (qa-1.md): PASS. All four gates green: typecheck (exit 0), lint (exit 0), tests (48/48 across 8 files), build (exit 0).

## Verdict: PASS
