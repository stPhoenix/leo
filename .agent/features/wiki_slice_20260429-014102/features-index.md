# Features index

| # | id | slug | name | purpose | deps | ui-needed | priority | covers |
|---|----|------|------|---------|------|-----------|----------|--------|
| 1 | F01 | wiki-bootstrap | Wiki bootstrap, layout, seeds, RAG exclusion | Idempotently create `wiki/` tree + `wiki-inbox.md`, seed `introduction.md` / `SCHEMA.md` / `index.md` / `log.md` once, register `wiki/` in RAG exclude list and `dirtyQueue` intake filter on plugin load. | — | no | high | FR-01, FR-02, FR-03, FR-04, FR-05, FR-06, NFR-09 |
| 2 | F02 | wiki-search-basics | `search_wiki` tool + LEO_PREAMBLE routing | Register read-only `search_wiki(query, opts?)`; index-first lexical match (N=8); extend `LEO_PREAMBLE` with knowledge-vs-lifestream routing rule. | F01 | no | high | FR-11, FR-12, FR-13 |
| 3 | F03 | wiki-status-slash | `/wiki-status` slash command | Read-only slash printing index size, last lint timestamp, live orphan count, current mutex state. | F01, F02 | yes | high | FR-52 |
| 4 | F04 | wiki-runtime-utils | Wiki budgets, logging namespaces, runId, live controller registry | Stand up `budgets.ts` (token caps), `loggingNamespaces.ts`, `runIdRegistry.ts` (`generateWikiRunId`), `liveControllerRegistry.ts` (Map<runId,…>). Pure utility scaffolding consumed by every subsequent feature. | — | no | high | NFR-03, NFR-10 |
| 5 | F05 | wiki-mutex | Vault-global wiki mutex | `WikiMutex.acquire/release/active` enforcing one ingest *or* lint at a time across threads; releases in outermost `try/finally`. | — | no | high | FR-23, FR-24, FR-25, NFR-05 |
| 6 | F06 | wiki-widget-framework | Live widget + terminal snapshot framework | `WikiWidgetController`, `WikiLiveBlock`, `WikiTerminalBlock`, `WikiTerminalSnapshot` (Zod), reload rehydrate to `error.code='reload'`. View-model surface for every ingest + lint phase. | F04 | yes | high | FR-48, FR-49, FR-50, FR-51, NFR-02 |
| 7 | F07 | wiki-search-warning | `search_wiki` in-progress warning | Inject `"warning: wiki <op> in progress (runId=…)"` into result + ≤1/min `Notice` toast per thread when wiki mutex is held. | F02, F05 | no | high | FR-14 |
| 8 | F08 | wiki-ingest-fetch-persist | Ingest FETCHING + PERSISTING + duplicate-detect | Per-source URL/vault/attachment fetch via inline-agent chain, SHA-256 + frontmatter raw write at `wiki/raw/<YYYYMMDD>-<slug>.md`, LangGraph `interrupt()` per source on sha collision (Skip / Re-process / Replace; default-Skip after 60 s). | F01, F04, F05 | no | high | FR-27, FR-28, FR-40, FR-41 |
| 9 | F09 | wiki-ingest-subagents | Ingest planner + extractor + reducer subagents | Single PLANNING LLM call; per-raw-entry EXTRACTING with semaphore (`extractorConcurrency`); per-page REDUCING with semaphore (`reducerConcurrency`); Zod retry-once-then-mark-error on parse failure. | F04, F08 | no | high | FR-29, FR-30, FR-31, NFR-06, NFR-07, NFR-08 |
| 10 | F10 | wiki-ingest-writer | Ingest writer | Apply reducer outputs deterministically: page creates → page edits → `sources/` summaries → regenerate `index.md` → append `log.md`; per-file atomic via `VaultAdapter`. | F04, F08, F09 | no | high | FR-32 |
| 11 | F11 | wiki-ingest-subgraph | Ingest subgraph FSM + refine sub-agent | LangGraph driver wiring PREPARING (refine) → FETCHING → PERSISTING → PLANNING → EXTRACTING → REDUCING → WRITING → DONE/CANCELLED/ERROR; cancel ≤2 s; mid-write completes in-flight file; mutex released in `finally`. | F04, F05, F06, F08, F09, F10 | no | high | FR-26, FR-33, FR-42, FR-43, FR-44, FR-45, FR-46, FR-47, NFR-01, NFR-05 |
| 12 | F12 | wiki-ingest-tool | `delegate_wiki_ingest` tool + `/wiki-ingest` slash | Built-in tool (`requiresConfirmation:true`) wrapping URL/vaultPath/attachment input, suspend-and-resume around subgraph terminal, busy-result on mutex held; slash command `/wiki-ingest`. | F11 | yes | high | FR-15, FR-16, FR-17, FR-18, FR-19, FR-52, NFR-04 |
| 13 | F13 | wiki-ingest-conversation | Conversation-kind ingest path | Extend `delegate_wiki_ingest` input union with `{kind:'conversation',title,body,citedSources?,note?}`; PERSISTING branch writes raw with `source:'conversation:<threadId>:<turnIndex>'`; FETCHING skipped; remaining phases unchanged. | F12 | no | high | FR-17 |
| 14 | F14 | wiki-inbox-tool | Inbox parser + `inbox_add` tool | Parse `wiki-inbox.md` checklist (`- [ ] <ref>  <!-- note? -->`); register read-only `inbox_add(ref, note?)` appending one line; tick / annotate-error primitives. | F01 | no | medium | FR-07, FR-08, FR-09 |
| 15 | F15 | wiki-inbox-batch | Inbox batch ingest path | `delegate_wiki_ingest({kind:'inbox'})` drains inbox sequentially (concurrency=1); on per-item terminal, ticks success or annotates error in `wiki-inbox.md`. | F12, F14 | no | medium | FR-10, FR-17 |
| 16 | F16 | wiki-lint-scan | Lint SCANNING phase | Enumerate `pages/` + `sources/`; build wikilink adjacency; count inbound/outbound; identify orphan pages and orphan raw entries. Skip `index.md` / `log.md` / `introduction.md`; treat `SCHEMA.md` as read-only checker input. | F01 | no | medium | FR-34 |
| 17 | F17 | wiki-lint-checkers | Lint CHECKING + PROPOSING phases | Checker subagents per concern (contradiction / stale / orphan / missing-page K=3 / missing-xref / research-gap with `suggestedQueries` + `severity:'info'` + `patch:null` / schema-drift); aggregate + rank into `LintPatch[]` + separate `schemaPatch`. | F04, F16 | no | medium | FR-35, FR-36 |
| 18 | F18 | wiki-lint-subgraph | Lint subgraph FSM | LangGraph driver wiring SCANNING → CHECKING → PROPOSING → CONFIRMING → WRITING → DONE/CANCELLED/ERROR; cancel ≤2 s; mid-write semantics; mutex released in `finally`; per-phase view-model fed to widget framework. | F04, F05, F06, F16, F17 | no | medium | FR-39, FR-42, FR-43, FR-44, FR-45, FR-46, FR-47, NFR-01, NFR-05 |
| 19 | F19 | wiki-lint-tool | Lint confirm UI + writer + `delegate_wiki_lint` + `/wiki-lint` slash | CONFIRMING multi-select findings list (Accept all / Reject all / Apply selected; schema patches confirmed per-run, never auto-applied); WRITING via ingest writer; tool with `requiresConfirmation:true`; `/wiki-lint` slash. | F10, F18 | yes | medium | FR-20, FR-21, FR-22, FR-37, FR-38, FR-52, NFR-04 |
